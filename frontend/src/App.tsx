import { RepositoryLookupPage } from './features/repository-lookup/RepositoryLookupPage'

// App is intentionally small right now. It is the top-level component where
// future routing/layout can be added when RepoPulse grows.
function App() {
  return <RepositoryLookupPage />
}

export default App
